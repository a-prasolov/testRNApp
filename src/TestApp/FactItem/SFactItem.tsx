import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#aaa',
    padding: 15,
    marginBottom: 10,
    borderRadius: 10
  },
  text: {
    fontSize: 16,
  }
})
